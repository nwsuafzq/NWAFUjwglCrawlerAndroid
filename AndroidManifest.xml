<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.nwafulive.zq.jwgl">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.RESTART_PACKAGES"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        
        android:theme="@style/AppTheme">
        <activity
            android:name="cn.nwafulive.zq.jwgl.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cn.nwafulive.zq.jwgl.ChooseCentre"
            android:label="西农查成绩-西农小助手">

        </activity>
        <activity
            android:name="cn.nwafulive.zq.jwgl.Course_list"
            android:label="西农查成绩-西农小助手">

        </activity>
        <activity
            android:name="cn.nwafulive.zq.jwgl.Score_find"
            android:label="西农查成绩-西农小助手">

        </activity>
    </application>

</manifest>
